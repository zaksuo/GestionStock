{% extends "BoutiqueConfigurationBundle::layout.html.twig" %}

{% block content %}
    <h4>Nouvelle campagne de remises</h4>

    <form action="{{ path('campagneremise_create') }}" method="post" {{ form_enctype(form) }}>
        {% include "BoutiqueConfigurationBundle:CampagneRemise:_form.html.twig" with {'form' : form} %} 
        <div class="right">
            <a class="btn" href="{{ path('campagneremise') }}">Annuler</a>
            <button class="btn btn-success" type="submit">Enregistrer</button>
        </div>
    </form>
{% endblock %}