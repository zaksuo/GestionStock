{% extends "BoutiqueConfigurationBundle::layout.html.twig" %}

{% block content %}
    <h1>TypeArticle creation</h1>

    <form action="{{ path('typeArticle_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('typeArticle') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}