{% extends "BoutiqueConfigurationBundle::layout.html.twig" %}

{% block content %}
    <h4>Liste des types de TVA</h4>

    <a href="{{ path('typeTva_new') }}" class="btn btn-success">Ajouter un type de TVA</a>
    <br /><br />
    <table class="table table-bordered table-striped">
        <tr>
            <th>ID</th>
            <th>Libellé</th>
            <th>Valeur pour calcul</th>
            <th>Actions</th>
        </tr>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.libelle }}</td>
                <td>{{ entity.valeur }}</td>
                <td class="action_menu width15">
                    <a href="{{ path('typeTva_edit', { 'id': entity.id }) }}"><div class="btn btn-small" title="Modifier les informations du type de TVA"><span class="ui-menu-icon icon_16 icon_16_notepad_edit"></span></div></a>
                </td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}