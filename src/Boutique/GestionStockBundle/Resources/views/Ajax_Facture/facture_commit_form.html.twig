

<form action="{{ path('facture_commit', {'id': facture.id}) }}" method="post">
    <div class="right">
        <a class="btn" href="{{ path('facture') }}">Retour</a>
        <input class="btn btn-success" id="facture_commit" type="submit" value="Finaliser la facture" />
    </div>
</form>