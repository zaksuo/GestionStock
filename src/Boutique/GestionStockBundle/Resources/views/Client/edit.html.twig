{% extends "BoutiqueGestionStockBundle::layout.html.twig" %}

{% block content %}
    <h1>Modification de fiche client</h1>

    <form action="{{ path('client_update', { 'id': client.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Modifier</button>
        </p>
    </form>

    <a href="{{ path('client') }}">Annuler</a> | 
    <a href="{{ path('client_delete', { 'id': client.id }) }}" >Supprimer</a>
    </form>
{% endblock %}