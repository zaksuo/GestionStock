{% extends "BoutiqueGestionStockBundle::layout.html.twig" %}

{% block content %}
    <h3>Modification de stock pour l'article " {{ article }} "</h3>
    <form class="form-horizontal " action="{{ path('stock_save', {'id_article': article.id, 'id_stock': stock.id}) }}" method="post" {{ form_enctype(form) }}>
        {% include 'BoutiqueGestionStockBundle:Stock:_form.html.twig' with {'article': article, 'form': form, 'edit': edit} %}
   
        <div class="right">
            <a class="btn" href="{{ path('article_show', {'id': article.id }) }}">Annuler</a>
            <button class="btn btn-success" type="submit">Enregistrer</button>
        </div>
    </form>
{% endblock content %}